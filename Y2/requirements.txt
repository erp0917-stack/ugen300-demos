# Gesture Page-Flip — Python packages
# Install: pip install -r requirements.txt
# Note: hailort (the package that talks to UGen300) is NOT included here.
#       Download the Windows build for Python 3.10 from the Hailo Developer Zone.

opencv-python      # webcam frames / drawing skeleton
numpy              # numerical ops
pyautogui          # simulate key presses to flip pages
